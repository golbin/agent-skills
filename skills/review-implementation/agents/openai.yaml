interface:
  display_name: "Review Implementation"
  short_description: "Fix root causes and simplify for real users"
  default_prompt: "Use $review-implementation to verify the user experience and implementation, fix root causes, and simplify the coherent end state."
