interface:
  display_name: "Review Implementation"
  short_description: "Review correctness, side effects, and simplicity"
  default_prompt: "Use $review-implementation to verify this implementation, then simplify it without weakening its purpose."
